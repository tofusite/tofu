[package]
name = "tofu_build"
version = "0.1.0"
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
pulldown-cmark = "0.8"
tera = "1.12"
tofu_frontmatter = { path = "../frontmatter" }
